# Qlik Script Effort Analyzer

## Installation steps (local)
1. Run `npm install`
2. Run `node index.js`

## Installation steps (docker)
1. Ensure docker and docker compose is installed
2. Run `docker-compose up --build`